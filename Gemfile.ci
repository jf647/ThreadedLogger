# our Gemfile is auto-generated using hoe-gemspec, which lumps all dependencies
# into runtime or dev categories.  It's not clear how I can auto-generate a gemspec
# that includes rake and minitest, but excludes debugger.  For now, I just manually
# create a stripped-down gemfile for Travis-CI
# 
# http://about.travis-ci.org/docs/user/languages/ruby/

source "https://rubygems.org/"

gem "rake", "~> 10.1.0"
gem "minitest", "~>5.0.8"
